<%= form_for(@shop)  do |f| %>
    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </p>
  

      <%= f.fields_for :business_opening_hours do |builder| %>
        # <% weekday = builder.object.day %>
          <%= render 'form_business_opening_hour', :f => builder ,:weekday => weekday%>
      <% end %>
 
  
    <div class="actions">
      <%= f.submit %>
    </div>
    
  <% end %>
  